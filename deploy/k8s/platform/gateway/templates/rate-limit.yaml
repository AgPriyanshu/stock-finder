apiVersion: gateway.nginx.org/v1alpha1
kind: SnippetsFilter
metadata:
  name: frontend-rate-limit
  namespace: gateway-ns
spec:
  snippets:
    - context: http
      value: |
        limit_req_zone $binary_remote_addr zone=frontend:10m rate=50r/s;
    - context: http.server.location
      value: |
        limit_req zone=frontend burst=100 nodelay;
        limit_req_status 429;
---
apiVersion: gateway.nginx.org/v1alpha1
kind: SnippetsFilter
metadata:
  name: backend-rate-limit
  namespace: gateway-ns
spec:
  snippets:
    - context: http
      value: |
        limit_req_zone $binary_remote_addr zone=backend_api:10m rate=30r/s;
    - context: http.server.location
      value: |
        limit_req zone=backend_api burst=60 nodelay;
        limit_req_status 429;
---
apiVersion: gateway.nginx.org/v1alpha1
kind: SnippetsFilter
metadata:
  name: s3-rate-limit
  namespace: gateway-ns
spec:
  snippets:
    - context: http
      value: |
        limit_req_zone $binary_remote_addr zone=s3_storage:10m rate=10r/s;
    - context: http.server.location
      value: |
        limit_req zone=s3_storage burst=20 nodelay;
        limit_req_status 429;
